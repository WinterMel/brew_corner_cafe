export default function CustomerLayout({ children }) {
  return (
    <div>
      {/* Navbar will go here later */}
      <main>{children}</main>
      {/* Footer will go here later */}
    </div>
  )
}